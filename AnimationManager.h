#pragma once
#include <SFML/Graphics.hpp>

/*
 *@brief ????????? ????????? ???????? ? ????.
 *
 *????? AAnimationManager ????????? ???????????????? ???????????? ?????? ????????
 * ?? ?????? ???????? ???????? ? ???????? ????? ??????.
 */
class AAnimationManager
{
public:

	AAnimationManager();

	~AAnimationManager();

	void AnimationUpdate(float DeltaTime);

	void SetCurrentFrame(const int Frame);

	void SetStopAtLastFrame(bool bStop);

	void ResetAnimation();

	bool IsAnimationFinished() const;

	int GetCurrentFrameIndex() const;

	sf::IntRect GetCurrentFrame() const;

	void SetReverse(bool reverse);
	void PlayForward();
	void PlayReverse();

private:
	bool bIsAnimationFinished;			 
	bool bStopAtLastFrame;				 
	bool bIsReverse;					 // Флаг для обратного воспроизведения

public:
	float FrameSpeed;					 

private:
	float CurrentFrameIndex;			 

public:
	std::vector<sf::IntRect> FrameRect;	 
	sf::Texture AnimTexture;			 
};
